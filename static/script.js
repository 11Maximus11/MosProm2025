document.addEventListener('DOMContentLoaded', () => { const form = document.getElementById('query-form'); const input = document.getElementById('query-input'); const output = document.getElementById('response-output'); const submitButton = document.getElementById('submit-button'); form.addEventListener('submit', async (event) => { event.preventDefault(); const userQuery = input.value; if (!userQuery.trim()) return; output.innerHTML = '<p class="loading">Обрабатываю запрос...</p>'; submitButton.disabled = true; input.disabled = true; try { const response = await fetch('/api/process_ticket', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: userQuery }), }); if (!response.ok) { throw new Error(`Ошибка сервера: ${response.status}`); } const data = await response.json(); output.innerHTML = `<p>${data.answer.replace(/\n/g, '<br>')}</p>`; } catch (error) { console.error('Ошибка:', error); output.innerHTML = `<p class="error">Произошла ошибка. Попробуйте еще раз.</p>`; } finally { submitButton.disabled = false; input.disabled = false; input.focus(); } }); });